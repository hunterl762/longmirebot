{
  "name": "longmirebot",
  "version": "2.0.0",
  "description": "Longmire Discord bot with a web dashboard for per-server command configuration",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "dev": "node --watch bot.js",
    "check": "node --check bot.js && node --check dashboard.js && node --check settings.js"
  },
  "engines": { "node": ">=20.0.0" },
  "author": "Hunter L.",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^14.27.0",
    "dotenv": "^18.0.3",
    "express": "^5.2.1",
    "express-session": "^1.19.0"
  }
}
